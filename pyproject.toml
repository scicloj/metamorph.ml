[tool.poetry]
description = ""
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.9"
mlflow = "2.10.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.mlflowaa]

[tool.poetry.scripts]
mlflowaa = 'mlflow.cli:cli'
